[buildout]
parts =
    python
    django
eggs =
    django-cms
    django
    South
develop =
    ../
versions = versions

[python]
recipe = zc.recipe.egg
interpreter = python
eggs = ${buildout:eggs}
scripts =


[django]
recipe = djangorecipe
version = 1.2.1
project = simple
settings = settings
eggs = ${buildout:eggs}

[versions]
South = 0.7.1
django = 1.2.1
